'use client';

export default function Page() {
  return (
    <div style={{
      minHeight: '100vh',
      display: 'grid',
      placeItems: 'center',
      color: '#fff',
      background: '#111'
    }}>
      <div>
        <h1 style={{margin: 0}}>Boring? — Flush the Shitcoins</h1>
        <p style={{opacity: .8}}>Route is working. We can wire the game next.</p>
      </div>
    </div>
  );
}
